import { Box, Flex, Avatar, HStack, VStack, CircularProgress, CircularProgressLabel, Heading } from "@chakra-ui/react";

export default function ProfilInfo(props: any) {
	return (
		<>
			
				
		</>
	)
}